---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section
  id={id}
  class:list={[
    "border-t border-primary/20 pt-6 pb-16 md:pt-8 md:pb-20",
    className,
  ]}
>
  <slot />
</section>
