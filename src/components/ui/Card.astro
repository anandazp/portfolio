---
import { Image } from "astro:assets";

const { src, alt, title, description } = Astro.props;
---

<div class="w-full pb-3 md:pb-5 bg-blackdark/10 space-y-2 rounded">
  <Image src={src} alt={alt} class="w-full rounded" />

  <div class="w-full px-3 md:px-5 space-y-3">
    <p class="font-medium text-xl md:text-2xl lg:text-3xl">{title}</p>
    <p class="font-inter">{description}</p>
    <slot />
  </div>
</div>
