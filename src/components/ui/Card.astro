---
const {src, alt, title, description} = Astro.props;
---

<div class="w-full pb-3 bg-graynord/20 space-y-2 rounded">
    <img src={src} alt={alt} class="w-full rounded">
    <div class="w-full px-3 space-y-2">
        <p class="font-semibold text-xl">{title}</p>
        <p class="">{description}</p>
        <slot/>
    </div>
</div>